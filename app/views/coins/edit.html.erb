<h1><%= t("titles.editing_coin")%></h1>

<%= render "form", coin: @coin %>

<br>

<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to t('links.show'), @coin, type:"button", class:"btn btn-secondary" %>
  <%= link_to t('links.back'), coins_path, type:"button", class:"btn btn-secondary" %>
</div>
